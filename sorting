Divide and Conquer
    recursively breaks down a problem into two or more, until the problem becomes simple.
    used for merge sort and quicksort

    Mergesort
        if N=1, then algorithm is complete
        recursively mergesort the first half and second half, resuling in two sorted halves
        merge two sorted halves into one sorted list
        
